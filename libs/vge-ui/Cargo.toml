[package]
name = "vge-ui"
version = "0.1.0"
edition = "2024"
description = "Shared client-side widget toolkit for VGE-aware terminal apps: accent theme, chrome paths, line editor, filterable picker, modal builders, key/mouse input parsing."

[dependencies]
vge-protocol = { path = "../../protocol/vge" }
unicode-width = "0.2.1"
